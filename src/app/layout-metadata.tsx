import { defaultMetadata } from './metadata';

// Export the metadata for the root layout
export const metadata = defaultMetadata;

// This is a Server Component that only exports metadata
// The actual layout implementation is in layout.tsx
